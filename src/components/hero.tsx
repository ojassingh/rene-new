import { FC } from "react";

const Hero: FC = () => {
  return (
    <section id="home" className="p-20">
      <h1 className="font-(--heading) text-5xl">Renesh</h1>
    </section>
  );
};

export default Hero;
